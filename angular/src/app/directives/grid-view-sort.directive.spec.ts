import { GridViewSortDirective } from './grid-view-sort.directive';

describe('GridViewSortDirective', () => {
  it('should create an instance', () => {
    const directive = new GridViewSortDirective();
    expect(directive).toBeTruthy();
  });
});
